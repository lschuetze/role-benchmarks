package test;

public team class BaseCallTeam {

    public class BaseRole playedBy BaseType {

        callin int noopRole(int arg) {
            int result = base.noopRole(arg);
            result += 1;
            return result;
        }

        noopRole <- replace retParam;
    }    
}
